"""示例入口：请使用 `python cli.py` 进行多轮交互。"""

import sys

if __name__ == "__main__":
    print("请运行：python cli.py", file=sys.stderr)
    print("可选参数：python cli.py --agent emotional", file=sys.stderr)
    raise SystemExit(0)
